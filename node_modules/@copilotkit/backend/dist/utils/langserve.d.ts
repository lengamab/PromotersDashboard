import { Action } from '@copilotkit/shared';
import { RemoteChain } from '../types/langserve.js';

declare function remoteChainToAction(chain: RemoteChain): Promise<Action<any>>;
declare function inferLangServeParameters(chain: RemoteChain): Promise<RemoteChain>;

export { inferLangServeParameters, remoteChainToAction };
