import * as _google_generative_ai from '@google/generative-ai';
import { Content, Tool, GenerativeModel } from '@google/generative-ai';
import { CopilotKitServiceAdapter, CopilotKitResponse } from '../types/service-adapter.js';
import { Message } from '@copilotkit/shared';

interface GoogleGenerativeAIAdapterOptions {
    /**
     * A custom `GenerativeModel` to use for the request.
     */
    model?: GenerativeModel;
}
declare class GoogleGenerativeAIAdapter implements CopilotKitServiceAdapter {
    private model;
    constructor(options?: GoogleGenerativeAIAdapterOptions);
    getResponse(forwardedProps: any): Promise<CopilotKitResponse>;
    transformMessages(messages: Message[]): Content[];
    transformMessage(message: Message): Content;
    transformTools(tools: any[]): _google_generative_ai.FunctionDeclarationsTool[];
    transformTool(tool: any): Tool;
}

export { GoogleGenerativeAIAdapter };
